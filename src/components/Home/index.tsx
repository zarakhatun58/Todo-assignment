import React from 'react';
import { useStyles } from './Styles';

const Home = () => {
    const classes=useStyles();
    return (
        <div>
            <h2>My Todo App</h2>
        </div>
    );
};

export default Home;